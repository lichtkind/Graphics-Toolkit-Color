ATTR

 ~name
 ~space
 @values

  new: ~name
       ~standard:name
       @RGB
       [ @RGB ]
       'SPACE' => 1,2,3,4
       ['SPACE' => 1,2,3,4]
       k => v, ..
       {k => v, ..}

-- getter --------------------------------------------------------------

  values: --> () | % | #hex
     --
     in: ~space
     as: ~format

  name: -- ~standard --> ~name
     ---

  closest_name: -- ~standard --> ~name
     ---

  distance:
     ---


-- single calc ---------------------------------------------------------

  set_value
     ---
     partial hash def

  add_value
     ---
     partial hash def

  mix
     ---
     with => full def
     factor => [0..1]
     space => name|rgb


  invert:
     ---
     ---
  ~space_name

-- set calc ------------------------------------------------------------

  complement
     ---
     ---
     +steps
     +tilt
     %|+target
     in: ~space_name # cylindrical only

  gradient
     ---
     to: $color_def
     ---
     +steps:
     +tilt:
     in: ~space_name

  cluster
     ---
     +min_disctance
     +radius => *
     ---
     in: ~space_name

